<% body_class "jobs-action" %>

<%= render 'shared/navbar_projet' %>

<section class="section-jobs mask-image">
  <div class="container">
    <div class="row">
      <div class="col s13 m6">
        <h2>Poste à<br>pourvoir</h2>
        <ul>
          <% @jobs.each do |job| %>
            <li><a href="https://docs.google.com/gview?url=<%= cloudinary_url(job.picture.path) %>&embedded=true", target="_blank"><%= job.name %></a></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</section>

<%= render 'shared/footer' %>
