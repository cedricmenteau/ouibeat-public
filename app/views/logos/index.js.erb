<% if @logos.present? %>
  $('.logo-container').append('<%= j render partial: "shared/section_logos", locals: {logos: @logos} %>');
  <% if params[:position] == nil %>
    $('.btn-more').addClass('disabled');
    $('.btn-more').attr('href', "#")
  <% else %>
   var href = $('.btn-more').attr('href').split('=')[0] + "=" + <%= params[:position] %>
    $('.btn-more').attr('href', href)
  <% end %>
<% end %>
